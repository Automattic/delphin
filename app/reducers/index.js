/**
 * External dependencies
 */
import { reducer as form } from 'redux-form';

/**
 * Internal dependencies
 */
import { checkout } from './checkout';
import { domainSearch } from './domain-search';

export default { checkout, form, domainSearch };
