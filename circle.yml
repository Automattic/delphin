deployment:
  staging:
    branch: master
    heroku:
      appname: delphin
  production:
    branch: master
    commands:
      - npm run prod:static
      - tar cvzf $CIRCLE_ARTIFACTS/public.tar.gz -C public .
