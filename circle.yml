deployment:
  staging:
    branch: master
    heroku:
      appname: delphin
  production:
    branch: add/circle-ci-artifact
    commands:
      - npm run prod:static
      - tar cvzf $CIRCLE_ARTIFACTS/public.tar.gz -C public .
