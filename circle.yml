machine:
  node:
    version: 5.6.0

deployment:
  production:
    branch: production
    commands:
      - ./deploy_prod.sh
  staging:
    branch: master
    heroku:
      appname: delphin
  production:
    branch: master
    commands:
      - npm run prod:static
      - tar cvzf $CIRCLE_ARTIFACTS/public.tar.gz -C public .
